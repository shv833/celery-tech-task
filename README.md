# celery-tech-task

Move files from `env/example` to `env/` and remove `.example` from names
Run project using `make cdev`
Open `http://localhost:3000` and wait for data updates
Check logs of `tests` container using command `make logs`
